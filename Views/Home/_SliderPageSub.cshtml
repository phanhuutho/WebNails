<div class="slider-banner">
    <div class="sliders">
        <div class="slider-1">
            <div class="box-slider">
                <div class="image">
                    <div class="container-xl">
                        <div class="row justify-content-center justify-content-md-end">
                            <div class="col-xl-4 col-lg-6 col-md-12">
                                <div class="box-inner text-center py-4">
                                    <h1 class="font-Prata" data-animation-in="animate__animated animate__backInLeft">Your Nails Specialist</h1>
                                    <div class="banner-button" data-animation-in="animate__animated animate__backInLeft">
                                        <a href="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? ViewBag.LinkBookingAppointment : ViewBag.HyperLinkTell)" target="_blank" class="btn btn-warning btn-theme btn-none-border">
                                            <span>@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "Book Now" : "Call Us")</span> <i class="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "fa fa-calendar" : "fa fa-long-arrow-right")"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="slider-2">
            <div class="box-slider">
                <div class="image">
                    <div class="container-xl">
                        <div class="row justify-content-center">
                            <div class="col-xl-4 col-lg-4 col-md-12">
                                <div class="box-inner text-center py-5">
                                    <h1 class="font-Prata" data-animation-in="animate__animated animate__backInLeft">All Services</h1>
                                    <div class="banner-button" data-animation-in="animate__animated animate__backInLeft">
                                        <a href="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? ViewBag.LinkBookingAppointment : ViewBag.HyperLinkTell)" target="_blank" class="btn btn-warning btn-theme btn-none-border">
                                            <span>@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "Book Now" : "Call Us")</span> <i class="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "fa fa-calendar" : "fa fa-long-arrow-right")"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="slider-3">
            <div class="box-slider">
                <div class="image">
                    <div class="container-xl">
                        <div class="row justify-content-center">
                            <div class="col-xl-4 col-lg-4 col-md-12">
                                <div class="box-inner text-center py-4">
                                    <h1 class="font-Prata" data-animation-in="animate__animated animate__backInLeft">E-Gift Purchase</h1>
                                    <div class="banner-button" data-animation-in="animate__animated animate__backInLeft">
                                        <a href="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? ViewBag.LinkBookingAppointment : ViewBag.HyperLinkTell)" target="_blank" class="btn btn-warning btn-theme btn-none-border">
                                            <span>@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "Book Now" : "Call Us")</span> <i class="@(!string.IsNullOrEmpty(string.Format("{0}", ViewBag.LinkBookingAppointment)) ? "fa fa-calendar" : "fa fa-long-arrow-right")"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
